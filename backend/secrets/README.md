Place your downloaded Noon service account JSON in this folder.

- Rename the file to `noon-service-account.json`.
- Expected local path: `backend/secrets/noon-service-account.json`.
- Never commit JSON keys, PEM keys, or service account credentials.
