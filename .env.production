# Do not point at a placeholder host — empty means “no baked-in API” at build time.
# Production deploys: set HR_PUBLIC_API_URL (see .env.deploy.example) so inject-api-runtime-config
# writes dist/api-runtime-config.js, or set a real URL here for a Vite-baked fallback.
# VITE_API_BASE_URL=
